generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_PRISMA_URL") // Vercel Postgres uses this var automatically
  directUrl = env("POSTGRES_URL_NON_POOLING") // For direct connections (migrations)
}

model Product {
  id          Int       @id @default(autoincrement())
  title       String
  description String
  price       Float
  images      String
  formats     String
  width       Int?
  height      Int?
  categoryId  Int?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  category    Category? @relation(fields: [categoryId], references: [id])
}

model Category {
  id       Int       @id @default(autoincrement())
  name     String    @unique
  products Product[]
}

model Order {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  status        String   // PAID, PENDING_BIZUM
  total         Float
  items         String
  customerEmail String?
  customerName  String?
  address       String?
  city          String?
  zipCode       String?
  phone         String?
}

model User {
  id        String   @id @default(cuid())
  username  String   @unique
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
